# N1MOX30 Mobile

Expo/React Native client foundation for Android, iOS and web.

Run:
```powershell
cd mobile
npm install
npx expo start
```

The mobile client uses the same N1MOX30 backend/API architecture. Configure the API base URL for a deployed environment before store submission.
